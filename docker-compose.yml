services:
  frontend:
    build:
      context: .
      args:
        - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
    image: ${DOCKERHUB_USERNAME:-appxcess}/meditation-frontend:${DOCKER_VERSION:-latest}
    container_name: frontend
    ports:
      - "3000:3000"
    restart: always
